# triage_client

A Flutter-based cross-device remote client for Triage.

## Overview
This is the human-interface remote client for Triage, supporting Web PWAs, iOS, Android, and Desktop environments from a single codebase. It connects to the `triaged` daemon via a secure WebSocket connection to observe terminal sessions and interact with the input lease model.

